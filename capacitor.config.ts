import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fe.vue.ionic',
  appName: 'fe-vue-ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
